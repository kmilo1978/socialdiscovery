# ==============================================================
# Social Discovery Engine - Environment Variables
# Copy this file to `.env.local` and fill in your key.
# ==============================================================

# --- SerpAPI (required for API mode; Basic mode and email validation need no key) ---
# Free tier: 100 searches/month, no credit card required.
# 1. Sign up: https://serpapi.com/users/sign_up
# 2. Copy your API key from the dashboard: https://serpapi.com/manage-api-key
# 3. Paste it below.
#
# To upgrade later (more searches/month), just change your plan on SerpAPI's
# pricing page (https://serpapi.com/pricing) — this same key keeps working,
# no code or env changes needed here.
SERPAPI_KEY=

# --- Multiple API Keys (optional, for more credits) ---
# Add multiple SerpAPI keys separated by commas to multiply your quota.
# The system rotates through them automatically (round-robin).
# Example: SERPAPI_KEYS=key1_here,key2_here,key3_here
# If set, SERPAPI_KEYS takes priority over SERPAPI_KEY.
SERPAPI_KEYS=

# --- Public API Key (optional, for external tool access) ---
# If set, external tools (n8n, Make, Zapier, Python scripts) can call your
# API endpoints by sending: Authorization: Bearer <this_key>
# or as query param: ?api_key=<this_key>
# Generate any random string (e.g. openssl rand -hex 32).
# If NOT set, the API only works from the web UI (no external access).
PUBLIC_API_KEY=
